<?php
/* Template Name: Add Texts */
// Get all $users

// Initial for each user add_user_meta - free_texts_remaining (initial value 1000), purchased_texts_remaining (initial value 0)
// Monthly if texts_remaining < 1000, texts_remaining = 1000, if texts_remaining > 1000, texts_remaining ???

